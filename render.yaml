services:
  - type: web
    name: heartofthecards-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    branch: main
    region: singapore
    envVars:
      - key: NODE_ENV
        value: production
